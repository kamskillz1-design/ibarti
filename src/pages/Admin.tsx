/**
 * Placeholder for the Admin portal — role-gated, built out in its own slice
 * (see plan section 8, "Safe meeting hubs & admin portal").
 *
 * The gate itself (checking profiles.role === 'admin') is not implemented
 * yet — this file exists only so the /admin route has something to point to
 * and the folder structure matches the plan.
 */
export default function Admin() {
  return <div>Admin — coming in its slice, role-gated</div>;
}


/**
 * Placeholder for the Explore page — built out in its own slice (see plan).
 * Exists now only so App.tsx has a working route to point to.
 */
export default function Explore() {
  return <div>Explore — coming in its slice</div>;
}

/**
 * Placeholder for the Home page — built out in its own slice (see plan).
 * Exists now only so App.tsx has a working route to point to.
 */
export default function Home() {
  return <div>Home — coming in its slice</div>;
}

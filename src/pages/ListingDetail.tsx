/**
 * Placeholder for the ListingDetail page — built out in its own slice (see plan).
 * Exists now only so App.tsx has a working route to point to.
 */
export default function ListingDetail() {
  return <div>ListingDetail — coming in its slice</div>;
}

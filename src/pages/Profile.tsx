/**
 * Placeholder for the Profile page — built out in its own slice (see plan).
 * Exists now only so App.tsx has a working route to point to.
 */
export default function Profile() {
  return <div>Profile — coming in its slice</div>;
}

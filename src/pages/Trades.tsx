/**
 * Placeholder for the Trades page — built out in its own slice (see plan).
 * Exists now only so App.tsx has a working route to point to.
 */
export default function Trades() {
  return <div>Trades — coming in its slice</div>;
}
